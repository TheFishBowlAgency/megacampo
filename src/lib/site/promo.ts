import { DEFAULT_HEADER } from "./defaults";

/** Fallback when header CMS content is unavailable. Prefer `useHeaderContent().promoMessage`. */
export const PROMO_BAR_MESSAGE = DEFAULT_HEADER.promoMessage;
